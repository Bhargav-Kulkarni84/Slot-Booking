<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shows Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
            color: #333;
        }
        h2 {
            text-align: center;
            color: #4CAF50;
            margin-bottom: 20px;
        }
        h4 {
            text-align: center;
            color: #555;
            margin-bottom: 30px;
        }
        /* Card styling for each slot */
        .slot-card {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 20px;
            margin: 15px 0;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .slot-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
        }
        /* Heading for slot number */
        h3 {
            margin: 0;
            font-size: 1.5em;
            color: #333;
        }
        /* Subheading for other slot details */
        h5 {
            margin: 10px 0;
            font-weight: normal;
            color: #666;
        }
        /* Styling for available and unavailable status */
        .available {
            color: #28a745; /* Green for available */
            font-weight: bold;
        }
        .unavailable {
            color: #dc3545; /* Red for unavailable */
            font-weight: bold;
        }
        /* Link styling */
        a {
            display: inline-block;
            margin-top: 15px;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        a:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <!-- Loop through the slots array -->
    <% for(slot of slots) { %>
        <div class="slot-card">
            <h3>Slot Number: <%= slot.slot_number %></h3>

            <h4>Slot Details</h4>

            <!-- Conditional display of slot availability -->
            <% if(slot.slot_isBooked === true) { %>
                <h5 class="unavailable">Status: Unavailable</h5>
            <% } else { %>
                <h5 class="available">Status: Available</h5>
            <% } %>

            <!-- Display slot duration and time -->
            <h5>Available Seats: <%= slot.available_seats %></h5>
            <h5>Duration: <%= slot.slot_duration %> mins</h5> 
            <h5>Time: <%= slot.start_time %> to <%= slot.end_time %></h5> 

            <!-- Link to view slot details -->
            <a href="/admin/slot/<%= slot.id %>">View Slot</a>
        </div>
    <% } %>
</body>
</html>
