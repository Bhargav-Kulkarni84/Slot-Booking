<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Slot Info</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
            color: #333;
        }
        h2 {
            text-align: center;
            color: #4CAF50;
            margin-bottom: 20px;
        }
        h4 {
            margin: 10px 0;
            padding: 10px;
            background-color: white;
            border: 1px solid #ddd;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: white;
            border-radius: 5px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        a {
            display: inline-block;
            margin-top: 15px;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        a:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2>Your Slot Details</h2>

        <h4>Slot Number: <%= user.slot.slot_number %></h4>
        <h4>Date: <%= user.slot.slot_date.toDateString() %></h4> 
        <h4>Time: <%= user.slot.start_time %> - <%= user.slot.end_time %></h4>
        <h4>Duration: <%= user.slot.slot_duration %> mins</h4>

        <h2>Your Credentials</h2>
        
        <h4>Username: <%= user.name %></h4>
        <h4>Number: <%= user.number %></h4>
        <h4>Email: <%= user.email %></h4>
        <h4>Reg-Number: <%= user.regno %></h4>
        
        <a href="/users">Go Back</a>
        <a href="/book">Book New Slot</a
        
        
    </div>

</body>
</html>
