<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Available Slots</title>
    <style>
        /* General Styles */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
            color: #333;
        }
        h2, h4 {
            text-align: center;
            color: #4CAF50;
            margin-bottom: 20px;
        }
        h4 {
            color: #555;
            margin-bottom: 30px;
        }

        /* Slot Card Styles */
        .slot-card {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 20px;
            margin: 15px 0;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .slot-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
        }

        /* Heading Styles */
        h3 {
            margin: 0;
            font-size: 1.5em;
            color: #333;
        }
        h5 {
            margin: 10px 0;
            font-weight: normal;
            color: #666;
        }

        /* Status Styles */
        .available {
            color: #28a745; /* Green for available */
            font-weight: bold;
        }
        .unavailable {
            color: #dc3545; /* Red for unavailable */
            font-weight: bold;
        }

        /* Button Styles */
        a {
            display: inline-block;
            margin-top: 15px;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        a:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h2>Available Slots</h2>
    <h4>Choose Slots From These Available Options</h4>

    <!-- Loop through each slot in the slots array -->
    <% for(let slot of slots) { %>
        <div class="slot-card">
            <!-- Slot Number -->
            <h3>Slot Number: <%= slot.slot_number %></h3>

            <!-- Slot Details Subheading -->
            <h4>Slot Details</h4>

            <!-- Conditional Rendering of Slot Status -->
            <% if(slot.slot_isBooked) { %>
                <h5 class="unavailable">Status: Unavailable</h5>
            <% } else { %>
                <h5 class="available">Status: Available</h5>
            <% } %>

            <!-- Slot Duration and Time -->
            <h5>Date: <%= slot.slot_date.toDateString()%></h5> 
            <h5>Duration: <%= slot.slot_duration %> mins</h5> 
            <h5>Time: <%= slot.start_time %> to <%= slot.end_time %></h5> 

            <!-- Book Slot Button (Only if Slot is Available) -->
            <% if(!slot.slot_isBooked) { %>
                <a href="/book/slot/<%= slot.id %>">Book Slot</a>
            <% } %>
        </div>
    <% } %>
</body>
</html>
